---
title: Hybrid Semantic and Spectral Ensemble for Robust Synthetic Image Source Attribution
title_zh: 混合语义与谱系集成方法用于鲁棒的合成图像来源归属
authors: Md. Ajwad Hossain
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22808v1"
tags: ["query:watermarking"]
score: 8.0
evidence: 文本到图像模型的合成图像源归属
tldr: "针对合成图像来源归因中训练与部署图像分布偏移（如JPEG压缩、模糊）的挑战，提出双分支集成框架。语义分支采用EfficientNet-B0结合指数移动平均和标签平滑，取证分支从高通噪声残差提取126种数学特征（含SVD谱和LBP），经截断SVD压缩后由XGBoost分类。在10个生成器数据集（55%测试集退化）上达到95.60%准确率，且全程无需GPU，仅CPU在6.5小时内完成端到端流程，展现了数学取证的实用性和可扩展性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22808v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1024, \"height\": 579, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.22808v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1268, \"height\": 613, \"label\": \"Table\"}]"
motivation: 现有合成图像来源归因方法难以应对真实部署中未知后处理操作（如JPEG压缩、模糊）导致的分布偏移。
method: 提出双分支集成：语义分支用EfficientNet-B0加EMA和标签平滑；取证分支从高通残差提取126种数学特征，经SVD压缩由XGBoost分类。
result: "在10个生成器数据集（55%测试集退化）上私有排行榜准确率95.60%，且无需GPU，CPU端到端6.5小时。"
conclusion: 融合语义与数学取证特征的混合框架在鲁棒性和计算效率上优于纯深度学习方案，适合实际部署。
---

## 摘要
文本到图像（T2I）模型的快速发展使得鲁棒的合成图像来源归属（SIA）方法成为必要。SIA中的一个关键挑战是原始训练图像与真实世界部署图像之间的分布偏移，后者会经历未知的后处理操作，如JPEG压缩和模糊。在这项工作中，我们针对ICANN 2026的DLMMDD挑战提出了一种双分支集成框架，融合了语义深度学习与数学取证特征提取。语义分支采用指数移动平均（EMA）和标签平滑正则化的EfficientNet-B0。取证分支从高通噪声残差中提取126个数学特征——包括SVD谱特征和局部二值模式——通过截断SVD进行压缩，并使用XGBoost进行分类。在包含10个生成器的数据集上（其中55%的测试集被退化），我们的方法在私有排行榜上达到了95.60%的准确率。此外，整个流水线计算效率极高，无需GPU加速，在标准CPU上端到端执行时间不到6.5小时，突显了数学取证在实际部署中的实用性和可扩展性。

## Abstract
The rapid advancement of text-to-image (T2I) models has necessitated robust Synthetic Image Source Attribution (SIA) methodologies. A critical challenge in SIA is the distribution shift between pristine training images and real-world deployed images, which undergo unknown post-processing operations such as JPEG compression and blurring. In this work, proposed for the DLMMDD Challenge at ICANN 2026, we introduce a dual-branch ensemble framework fusing Semantic Deep Learning with Mathematical Forensic Feature Extraction. The semantic branch employs EfficientNet-B0 regularized with Exponential Moving Averaging (EMA) and Label Smoothing. The forensic branch extracts 126 mathematical features -- including SVD spectral profiles and Local Binary Patterns -- from high-pass noise residuals, compressed via Truncated SVD and classified with XGBoost. Evaluated on a dataset of 10 generators where 55% of the test set is degraded, our approach achieves a private leaderboard accuracy of 95.60%. Furthermore, the entire pipeline is highly computationally efficient, requiring no GPU acceleration and executing end-to-end on a standard CPU in under 6.5 hours, highlighting the practicality and scalability of mathematical forensics for real-world deployment.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **研究动机**：随着文本到图像（T2I）生成模型（如Stable Diffusion系列）质量迅速提升，迫切需要能够鲁棒地追溯合成图像来源的归属方法（SIA）。当前SIA系统面临的核心挑战是**训练数据（干净合成图像）与真实部署图像之间的严重分布偏移**——真实部署图像常遭受未知的后处理操作，如JPEG压缩、高斯模糊、对比度调整等。
- **具体问题**：DLMMDD Challenge（ICANN 2026）中，训练集为7000张干净的合成人脸图像，但测试集有3000张图像，其中55%被施加了复合退化。纯深度学习方法容易对训练集中的高频像素噪声过拟合，在退化样本上性能急剧下降。
- **整体含义**：需要一种不依赖GPU、计算高效且能抵抗分布偏移的混合方法，融合语义理解与数学取证，以便在实际场景中部署。

## 2. 提出的方法论

### 核心思想
构建一个**双分支集成框架**，同时利用：
- **语义分支**：基于EfficientNet-B0的深度学习，捕捉生成器特有的高层语义指纹。
- **取证分支**：基于数学特征的确定性流水线，从噪声残差和频域中提取生成器固有的物理指纹。

两个分支的输出通过加权软投票融合，最终作出归属决策。

### 关键技术细节

#### 语义分支：EfficientNet-B0 + EMA + 标签平滑
- **架构**：使用在ImageNet预训练的EfficientNet-B0，因其SE块能动态加权特征通道，擅长隔离稀疏的生成器异常。
- **正则化策略**：
  - **指数移动平均（EMA）**：维护一个影子网络，权重更新衰减因子α=0.999，推理时仅使用EMA权重，消除批次级方差和过拟合。
  - **标签平滑（Label Smoothing）**：平滑因子0.1，抑制模型对干净样本的过度自信，强制学习更泛化的表示。
  - **训练饱和控制**：严格在16个epoch后停止训练，避免记忆噪声伪像。
- **推理增强**：采用测试时增强（TTA），对输入图像做水平翻转并平均logits。

#### 取证分支：频谱SVD + XGBoost
- **特征提取（126维）**：
  1. 将图像转换为YCbCr颜色空间。
  2. 对Y、Cb、Cr三个通道分别提取噪声残差：`Nc = c - MedianBlur(c, 5)`。
  3. 对每个通道噪声残差进行奇异值分解（SVD），取前30个奇异值，并计算谱衰减的一阶差分均值μ(Δs)和方差σ²(Δs)。
  4. 在Y通道上计算LBP直方图并追加。
  5. 计算三个通道噪声残差间的交叉相关性（ρ(NY,NCb)等）。
  6. 计算Y通道DCT系数的AC部分方差。
- **特征压缩与分类**：
  - 使用截断SVD将126维特征压缩至125维（保留100%方差）。
  - 基于5折交叉验证训练XGBoost分类器。

#### 集成策略
- 加权软投票：`P_final = 0.73 * P_EffNet + 0.27 * P_XGBoost`
- 权重经实验校准：深度学习分支占主导（73%），取证分支作为安全网（27%），在退化严重时自动增强取证分支影响力。

## 3. 实验设计

### 数据集与场景
- **数据集**：DLMMDD Challenge官方数据集，包含10个不同的T2I生成器。
- **训练集**：7000张干净合成人脸图像。
- **测试集**：3000张图像，其中**55%受到复合退化**（如JPEG压缩、模糊等），其余45%为干净图像。
- **评估指标**：分类准确率（accuracy）。

### 基准对比
- 论文未列出具体对比方法名称，但提到：
  - 独立评估了语义分支（EfficientNet-B0）和取证分支（SVD-XGBoost）各自的准确率。
  - 独立EfficientNet-B0准确率约95.5%。
  - 独立取证分支准确率约85%。
  - 集成后私人排行榜准确率95.60%，相比纯CNN提升约1.1个百分点。

### 排行榜表现
- 公共排行榜准确率：96.66%
- 私人排行榜准确率：95.60%（排名38/80+队伍）
- 公私准确率差异仅1.06%，说明模型泛化性良好。

### 实验充分性评价
- **消融实验**：通过比较独立分支与集成性能，验证了集成策略的有效性（+1.1%）。
- **错误分析**：分析了两种主要失败模式（同族生成器混淆、复合退化下取证分支失效）。
- **可复现性**：设置全局种子42，约束所有库的可复现性。
- **局限性**：未进行跨数据集迁移实验（如从DLMMDD数据集迁移到其他合成图像数据集），也未针对不同退化类型做细粒度分析（仅定性描述）。

## 4. 资源与算力

- **硬件**：全程在**标准CPU**上运行（Intel Xeon 2.20GHz），通过Kaggle Notebook执行，**零GPU加速**。
- **执行时间**：整个流水线端到端精确耗时 **6小时9分钟**。
  - EfficientNet-B0训练（16 epochs）：约5小时40分钟
  - EfficientNet-B0推理（含TTA）：约5分钟
  - 取证特征提取（训练+测试）：约25分钟
  - SVD压缩与XGBoost训练（5折CV）：<1分钟
- **无GPU需求**是本工作的一大亮点，证明了数学取证方案的资源效率。

## 5. 实验数量与充分性

- **实验数量**：文中主要呈现了以下实验结果：
  - 公共/私人排行榜准确率（最终结果）
  - 语义分支独立性能（~95.5%）
  - 取证分支独立性能（~85%）
  - 集成后性能（95.60%）
- **充分性评价**：
  - **优点**：通过独立分支对比验证了集成的必要性；通过公私榜差异验证了泛化性；通过错误分析揭示了系统边界。
  - **不足**：
    - 未报告每个生成器类别的细粒度准确率（混淆矩阵缺失）。
    - 未针对不同退化类型（如JPEG、模糊、噪声）分别测试鲁棒性。
    - 未与其他公开的SIA方法（如纯频率方法、其他混合方法）做定量对比。
    - 未进行超参数敏感性分析（如集成权重0.73:0.27的搜索过程仅提及“empirically proven”）。
    - 消融实验仅对比了有无集成，未分别消融EMA、标签平滑、训练饱和、取证特征子集等设计。

## 6. 主要结论与发现

- 提出的混合语义-谱系集成方法在极度分布偏移（55%测试集严重退化）下实现了**95.60%的私人排行榜准确率**，且公私榜准确率差异仅1.06%，表明**结构鲁棒性**。
- 取证分支在遭受剧烈JPEG压缩的图像子集上精度优于语义分支，而语义分支在干净样本上占优。加权集成能动态平衡两者，带来显著的统计提升（+1.1%）。
- 整个流水线**无需GPU**，在CPU上6.5小时内完成，证明数学取证在资源受限场景下的实用性和可扩展性。
- 主要失败模式：同族生成器内部混淆（共享VAE解码器）和复合退化下噪声残差被覆盖。

## 7. 优点

1. **创新性混合架构**：将深度学习语义特征与确定性数学取证（SVD谱、LBP、DCT等）有机融合，正交互补性强。
2. **极高的计算效率**：完全CPU运行，训练+推理<6.5小时，适合实际部署。
3. **强鲁棒性**：在55%测试样本严重退化的极端条件下，公私榜准确率仅下降1.06%，远超传统纯CNN方案。
4. **设计简洁而有效**：EMA+标签平滑+训练饱和组合有效防止高频过拟合；集成权重经经验校准，操作简单。
5. **充分的可复现性**：固定随机种子，约束所有库。
6. **开源友好**：基于Kaggle公开数据集和常用工具，便于后续研究复现和扩展。

## 8. 不足与局限

1. **实验覆盖不全面**：缺乏与其他基线方法的详细对比（仅自对比）；未提供每个生成器类别的混淆矩阵；未对不同退化类型做细粒度评估。
2. **消融实验不充分**：未单独验证EMA、标签平滑、训练饱和、SVD谱、LBP等每个组件对最终性能的贡献；集成权重的选择仅描述为“empirically proven”，缺乏搜索细节。
3. **可迁移性未知**：仅在一个特定挑战数据集上验证，未测试在其他生成器集或跨图像域（如自然场景）上的泛化能力。
4. **错误模式未完全解决**：同族生成器混淆和复合退化下取证失效是系统性缺陷，作者虽有分析但未提出有效缓解方案。
5. **潜在偏差风险**：训练数据仅含人脸图像，可能引入人脸特有伪影的偏差；10个生成器可能不足以覆盖所有T2I模型变体。
6. **公平性**：排名38/80+，虽性能较高但并非顶尖，论文未与排名更高的方法进行局限性比较。

（完）
