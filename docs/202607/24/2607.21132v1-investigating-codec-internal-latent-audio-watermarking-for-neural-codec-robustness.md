---
title: Investigating Codec-Internal Latent Audio Watermarking for Neural Codec Robustness
title_zh: 研究面向神经编解码器鲁棒性的编解码器内部潜在音频水印技术
authors: "Zi Hu, Houmin Sun, Linxi Li, Yechen Wang, Liwei Jin, Carsten Maple, Ming Li"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21132v1"
tags: ["query:watermarking"]
score: 8.0
evidence: 研究神经音频编解码器中潜在空间水印的鲁棒性
tldr: "神经音频编解码器因重编码、量化和重合成对水印构成挑战。本文提出在连续潜空间中嵌入水印，使用SEANet编码器-解码器、Conformer嵌入器和RVQ引导潜分解，并在潜域训练检测器。在48kHz语音上，EnCodec感知训练将EnCodec-24k比特准确率从78.8%提升至95.6%和97.1%，但PESQ从3.727降至3.514和3.427。揭示了将水印载体移至神经解码前潜空间的权衡，为鲁棒水印研究提供新视角。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21132v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1835, \"height\": 792, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21132v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 920, \"height\": 127, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21132v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 921, \"height\": 162, \"label\": \"Table\"}]"
motivation: 针对神经音频编解码器对传统水印的挑战，研究在编解码器内部潜空间嵌入水印的可行性。
method: 采用SEANet编码器-解码器、Conformer嵌入器和RVQ引导潜分解，在连续潜空间嵌入32位消息，并在潜域训练检测器。
result: "在48kHz语音上，EnCodec感知训练使EnCodec-24k比特准确率从78.8%提升至95.6%和97.1%，但PESQ从3.727下降至3.514和3.427。"
conclusion: 将水印载体移至神经解码前的潜空间可显著提升编解码器鲁棒性，但需权衡水印不可感知性。
---

## 摘要
神经音频编解码器对音频水印来说是具有挑战性的变换，因为它们会重新编码、量化和重新合成语音。本文研究用于编解码器鲁棒性的连续潜在空间水印技术。我们不只在波形或频谱图上添加水印，而是将32位消息嵌入到类似编解码器的语音自编码器的连续潜在表示中。该流程使用了SEANet风格编码器-解码器、基于Conformer的消息嵌入器、RVQ引导的潜在分解，以及在信号处理和神经编解码器变换下训练的潜在域检测器。我们并非提出最终的通用水印基线，而是刻画当水印载体被移入神经解码之前时出现的权衡。在48 kHz语音上，EnCodec感知训练将EnCodec-24k比特准确率从78.8%提高到95.6%和97.1%，而PESQ从3.727下降到3.514和3.427。

## Abstract
Neural audio codecs are challenging transformations for audio watermarking because they re-encode, quantize, and resynthesize speech. This paper investigates continuous latent-space watermarking for codec robustness. Instead of adding a watermark only to the waveform or spectrogram, we embed a 32-bit message into the continuous latent representation of a codec-like speech autoencoder. The pipeline uses a SEANet-style encoder-decoder, a Conformer-based message embedder, RVQ-guided latent decomposition, and a latent-domain detector trained under signal-processing and neural-codec transformations. Rather than proposing a final universal watermarking baseline, we characterize the trade-offs that appear when the watermark carrier is moved before neural decoding. On 48 kHz speech, EnCodec-aware training improves EnCodec-24k bit accuracy from 78.8% to 95.6% and 97.1%, while PESQ decreases from 3.727 to 3.514 and 3.427.

---

## 论文详细总结（自动生成）

### 论文详细总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：神经音频编解码器（如 EnCodec、HiFi-Codec）通过对波形进行“分析‑量化‑合成”操作，会严重破坏传统添加在波形或频谱图上的水印信号。现有深度水印方法在应对这类变换时鲁棒性不足。
- **研究动机**：探索将水印嵌入到编解码器内部的**连续潜在表示**中，使水印在神经解码前就已经与内容特征融合，从而降低编解码器对水印的破坏。
- **整体含义**：本文并非提出最终通用水印方案，而是系统性地刻画在神经解码前进行潜空间嵌入时的**质量‑鲁棒性权衡**，为后续研究提供设计参考。

#### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将32位消息嵌入到一个类似 EnCodec 的语音自编码器的连续潜变量中，而不是在波形或频谱层添加扰动。
- **关键技术细节**：
  - **整体流程**：输入语音 \( x \) → SEANet 风格编码器 \( E_\theta \) → 连续潜表示 \( z = \tanh(E_\theta(x)) \) → 消息条件嵌入器 \( W_\psi \) 预测有界残差 \( \Delta \) → 水印潜变量 \( z_{\text{wm}} = z_{\text{base}} + \alpha \Delta \) → 解码器 \( G_\theta \) 重建波形 \( \hat{x} \) → 攻击后重新编码 → 潜域检测器 \( D_\phi \) 恢复消息。
  - **嵌入机制**：
    - 早期尝试了**乘法掩码**（不稳定）和**硬 RVQ 瓶颈**（难收敛），最终采用**有界加性残差**（公式7），并通过 \( \tanh \) 限制扰动幅度。
    - RVQ 不作为强制重构瓶颈，而是用作**引导与分解**：将潜变量分解为前端保护部分 \( z_{\text{front}} \)（前 M 个 RVQ 分量）和后端连续部分 \( z_{\text{back}} \)，水印只注入后端（公式10），并用投影避免扰动前端。
  - **消息嵌入器**：基于 Conformer 模块，通过 FiLM 风格特征调制将消息条件融入潜序列处理。
  - **检测器**：在潜域训练，输入为攻击后波形重新编码的潜表示 \( z' = \tanh(E_\theta(A(\hat{x}))) \)。
  - **损失函数**（公式13）：包含多尺度 mel 谱损失、MRSTFT 损失、时频响度比损失、对抗损失、潜一致性损失、消息 BCE 损失和 RVQ 损失，分阶段训练（早期用代理潜检测，后期引入真实攻击路径）。
  - **直通估计器**：对 EnCodec 攻击前向传播使用真实编码输出，反向传播近似为恒等梯度，使检测器梯度有效传递。

#### 3. 实验设计：数据集、benchmark、对比方法

- **数据集**： Emilia 数据集的英语子集（大规模多语言语音数据集），音频重采样/加载为 48 kHz，截取或填充至 120000 样本。
- **Benchmark**：
  - **质量指标**：PESQ（主要）、ViSQOL、STOI、SNR/SI-SDR（辅助）。
  - **鲁棒性指标**：比特准确率（即 1-BER），聚合所有比特和话语后的平均恢复率。
  - **攻击集**：
    - 信号处理组：加性噪声、样本抑制、低通/高通/带通滤波、重采样、幅度缩放、量化、平滑、粉红噪声、混响、时间拉伸、随机相位偏移、频谱图增强、片段替换。
    - 神经编解码器组：EnCodec-24k（主要条件）、EnCodec-16k（更强压力条件）、HiFiCodec-24k（额外参考）。
- **对比方法**：
  - 论文未直接对比其他水印方法（但提及 AudioSeal 作为相关系统，强调设计目标不同），主要进行**自身变体对比**：
    - 不同训练策略：平衡训练 → EnCodec-24k 聚焦训练 → EnCodec-重采样终点（攻击采样概率逐步偏向 EnCodec）。
    - 骨干控制：SEANet 纯自编码器 vs. DAC 风格纯自编码器 vs. DAC 风格完整流水线（替换 SEANet 为 DAC 结构）。

#### 4. 资源与算力
- **文中未明确说明**使用的 GPU 型号、数量或训练时长。仅提及训练步数（如 258k、222k、276k），但未给出单步时间或总时长。未提及算力资源细节。

#### 5. 实验数量与充分性
- **实验数量**：
  - 主实验：3 种训练策略下的质量与 EnCodec-24k 鲁棒性对比（表 I）。
  - 非编解码器攻击鲁棒性：单一检查点（EnCodec-24k 聚焦）报告了 14 种攻击下均 >98% 比特准确率。
  - 骨干控制实验：3 组（SEANet plain AE、DAC plain AE、DAC full pipeline，表 II）。
  - 诊断实验：去除水印残差的“无嵌入”条件对比（文本描述）。
  - 编解码器重构参考：无 watermark 下的 EnCodec-24k / HiFiCodec-24k PESQ。
- **充分性与公平性**：
  - **优点**：覆盖了质量、鲁棒性、骨干兼容性多个维度，设计了变体和控制实验来分离水印扰动与骨干重构能力的影响。
  - **不足**：
    - 未与现有水印方法（如 WavMark、AudioSeal、SilentCipher）直接比较，作者解释为设计目标不同（这些方法在波形域嵌入，本文在潜空间嵌入），但缺少定量对比削弱了说服力。
    - EnCodec-16k 压力条件结果差（比特准确率接近随机），但仅提及一次未进一步分析原因或改进尝试。
    - 未进行消融实验：例如分别去掉 RVQ 引导、前端保护、潜一致性损失等对性能的影响。
    - 评估指标方面：PESQ 在 48 kHz 输入下需要下采样，可能存在偏差；未使用 MOS 主观测试。

#### 6. 论文的主要结论与发现
- **核心权衡**：增加 EnCodec-24k 训练权重可显著提升比特准确率（从 78.8% 至 95.6% / 97.1%），但代价是 PESQ 从 3.727 下降至 3.514 / 3.427。ViSQOL 变化幅度较小。
- **骨架兼容性**：更强的独立自编码器（DAC 风格）并不能直接转化为更好的水印流水线——完整流水线中 DAC 风格效果并未优于 SEANet 风格，说明骨架需要与嵌入器和检测器联合优化。
- **分离效果**：去除推理时的水印残差对 PESQ 影响很小（平衡训练下 3.734 vs 3.727），但聚焦训练下甚至出现无嵌入 PESQ 更低的情况，说明水印残差与自编码器权重、攻击目标耦合复杂。
- **局限性总结**：EnCodec-16k 仍高度破坏性，水印鲁棒性不能自动迁移；连续潜空间方案在离散编码 token 层面尚未探索。

#### 7. 优点
- **问题新颖且针对性强**：聚焦神经编解码器这一特定且破坏性大的变换，与常见信号处理攻击区分开来，具有重要实践意义。
- **方法论系统**：不直接推出最终方案，而是比较多种潜空间嵌入变体（乘法/加性、有/无硬 RVQ 瓶颈、前端保护），并清晰说明各尝试的失败原因，体现学术诚实。
- **可控的权衡分析**：通过逐步改变 EnCodec 采样概率，精细刻画质量与鲁棒性的定量关系，为实际部署提供指导。
- **骨架诊断实验**：通过纯自编码器基线和水印流水线变体，分离了骨干重构能力与联合训练的影响，这是很多工作忽略的。

#### 8. 不足与局限
- **缺乏同类方法对比**：未在统一设置下与主流水印方法（如 AudioSeal、WavMark）比较，无法判断本文方法的 relative 优势，削弱了实验充分性。
- **压力条件分析不足**：EnCodec-16k 鲁棒性极差但未深入分析原因或尝试改进（如 16 kHz 训练），也未提及其他编解码器（如 HiFiCodec-24k 低 PESQ 下的鲁棒性）。
- **消融实验缺失**：未对 RVQ 引导、前端保护、潜一致性损失等组件进行单独消融，难以断定各模块贡献。
- **主观评估缺位**：仅靠 PESQ/ViSQOL 客观指标，缺少 MOS 等主观听感验证，尤其在水印扰动可能引入 artifact 时。
- **测试集与泛化性**：仅用 Emilia 英语子集，未测试其他语言、不同音质、背景噪声等场景，泛化性未知。
- **计算资源未报告**：无法复现或估计训练成本，不利于研究社区比较和改进。

（完）
