---
title: On the Information-Theoretic Limits of Latent-Space Watermarking Through Pretrained Generators
title_zh: 论通过预训练生成器的潜空间水印的信息论极限
authors: "Jinwan Jeon, Minju Lee, Sung Hoon Lim"
date: 2026-09-21
pdf: "https://arxiv.org/pdf/2609.24377v1"
tags: ["query:watermarking"]
score: 9.0
evidence: 通过预训练生成器的潜在空间水印信息论极限
tldr: 本文研究通过预训练生成器的潜在空间水印：编码器用消息和密钥选择潜在输入，经 renderer 生成输出，并要求每个消息与语义上下文下输出条件分布精确保持目标分布。作者在有限字母表下给出速率—密钥内界与外界，并在潜在分布可由输出分布唯一确定时得到容量区域；随后推广到联合高斯与矢量高斯模型，分析密钥资源分配。还引入再生攻击，刻画单轮复合容量及多轮重复再生的容量衰减。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.24377v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1187, \"height\": 398, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.24377v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 873, \"height\": 465, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.24377v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 981, \"height\": 647, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.24377v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 978, \"height\": 651, \"label\": \"Figure\"}]"
motivation: 生成式水印需在保持预训练生成器输出分布的同时嵌入可检测消息，并厘清潜在接口的编码与协调极限及再生攻击威胁。
method: 将水印建模为经 renderer 的潜在到输出随机映射，推导有限字母表速率—密钥界、联合/矢量高斯充分统计量，并把再生攻击纳入复合容量框架。
result: 有限字母表下给出内外界；唯一可逆时得到容量区域；高斯模型刻画密钥按模式最优分配；标量高斯下得到单轮复合容量及多轮再生容量衰减。
conclusion: 结果为预训练生成器中的潜在空间水印提供信息论极限，揭示分布保持、密钥协调与再生鲁棒性之间的基本权衡。
---

## 摘要
我们研究通过预训练生成器进行的潜空间水印，其中使用一个规定的从潜变量到输出的随机映射，称为渲染器。水印编码器利用消息和密钥选择潜变量输入。对于每个消息和语义上下文，发布输出必须恰好具有期望的条件输出分布。对于有限字母表，我们推导速率--密钥内界和外界，并刻画通过规定潜变量接口实现水印通信所需的编码与协调要求。当生成器的目标输出分布通过渲染器唯一确定相应的潜变量输入分布时，一个加强的逆定理给出容量区域；同一区域也支配着对预训练潜变量分布的显式保持。我们将分析扩展到一般的联合高斯模型，并识别出潜变量的一个充分统计量，它同时刻画了生成输出中可用的承载水印信息，以及为保持其目标分布所需的潜变量协调。对于向量高斯模型，我们进一步刻画密钥资源在所得各模式间的最优分配。最后，我们转向一种在生成式水印中尤为自然的新兴鲁棒性威胁：对手可以重新生成已发布样本，以获得同一底层内容的新实现，同时减弱或破坏嵌入的水印。我们将这一鲁棒性维度纳入框架，并刻画标量高斯模型的一次复合容量，其中语义上下文对编码器已知但对检测器隐藏，而再生成攻击可能依赖于该上下文。将分析扩展到多轮重复的规范再生成，我们刻画由此导致的水印容量衰减。

## Abstract
We study latent-space watermarking through a pretrained generator using a prescribed latent-to-output stochastic mapping, called the renderer. A watermark encoder selects the latent input using a message and secret key. For every message and semantic context, the released output must have exactly the desired conditional output distribution. For finite alphabets, we derive rate--key inner and outer bounds and characterize the coding and coordination requirements for realizing watermark communication through the prescribed latent interface. When the target output distribution of the generator uniquely determines the corresponding latent input distribution through the renderer, a strengthened converse yields the capacity region; the same region governs explicit preservation of the pretrained latent distribution. We extend the analysis to general jointly Gaussian models and identify a sufficient statistic of the latent that captures both the watermark-bearing information available at the generated output and the latent coordination required to preserve its target distribution. For the vector Gaussian model, we further characterize the optimal allocation of the secret-key resource across the resulting modes. Finally, we turn to an emerging robustness threat that is particularly natural in generative watermarking: an adversary can regenerate the released sample to obtain a fresh realization of the same underlying content while attenuating or destroying the embedded watermark. We incorporate this robustness axis into our framework and characterize the one-pass compound capacity of the scalar Gaussian model when the semantic context is known to the encoder but hidden from the detector, while the regeneration attack may depend on that context. Extending the analysis to multiple rounds of repeated canonical regeneration, we characterize the resulting watermark-capacity decay.

---

## 论文详细总结（自动生成）

## 一、核心问题与整体含义

- **研究背景**：生成模型广泛用于图像、音频、视频、文本生成，但生成内容的来源与真实性难以验证。水印通过在生成过程中嵌入可检测信息来缓解该问题。
- **潜空间水印的特点**：不修改预训练生成器，而是在其潜变量接口中嵌入信息，例如修改扩散初始噪声或对高斯潜变量做分布保持采样。优点是无需重设计生成管线，且可尽量保持原生成行为。
- **核心困难**：仅选择可区分的潜输入并不够。水印在潜空间嵌入后，必须经过一个水印设计者无法控制的固定随机映射，即“渲染器” \(q_r(x|u,s)\)，才能到达公开输出 \(X\)。合法检测器只能看到输出 \(X^n\) 和密钥 \(K\)，看不到语义上下文 \(S^n\)。
- **核心问题**：在通过预训练生成器的固定潜接口嵌入水印时，可靠通信的基本极限是什么？同时必须对每个消息和语义上下文精确保留目标条件输出分布 \(q_0(x|s)\)。
- **整体含义**：论文将生成式水印建模为“固定渲染器下的信息嵌入 + 精确分布保持 + 有限密钥协调”问题，给出有限字母表、唯一预像、联合高斯/向量高斯、以及再生攻击鲁棒性的信息论极限。它强调：水印容量不仅取决于潜变量本身，还取决于渲染器暴露了潜变量的哪些方向，以及密钥如何协调潜分布。

## 二、方法论：核心思想与关键技术细节

### 1. 问题建模

- **预训练生成器**：
  - 语义/上下文 \(S\)，物理潜变量 \(U\)，公开输出 \(X\)。
  - 预训练系统联合分布：
    \[
    q_{\text{pre}}(s,u,x)=q_0(s)q_{\text{lat}}(u|s)q_r(x|u,s)
    \]
  - \(q_0(s,x)\) 是期望输出分布，\(q_{\text{lat}}(u|s)\) 是预训练潜先验，\(q_r(x|u,s)\) 是固定渲染器。
- **水印编码器**：
  - 消息 \(M\sim \text{Unif}[1:2^{nR}]\)，密钥 \(K\sim \text{Unif}[1:2^{nR_k}]\)。
  - 编码器为条件分布 \(p(u^n|s^n,m,k)\)，可跨块耦合，可随机化。
- **检测器**：
  - 仅观察 \(X^n\) 与密钥 \(K\)，不观察 \(S^n\)，输出 \(\hat M=g_n(X^n,K)\)。
- **性能要求**：
  - 可靠解码：\(\Pr\{\hat M\ne M\}\to 0\)。
  - 精确输出保持：对每个消息 \(m\)，
    \[
    p(x^n|s^n,m)=\prod_{i=1}^n q_0(x_i|s_i)
    \]
    即密钥平均后的诱导输出分布必须精确等于目标条件分布。
  - 这是消息级、上下文条件、有限块精确保持，强于渐近总变差消失准则。

### 2. 有限字母表速率–密钥界

- 引入辅助随机变量 \(V\)，构造条件分布 \(p(v,u|s)=p(v|s)p(u|s,v)\)。
- 可行分布类：
  \[
  \mathcal P(q_0;q_r)=\{p(s,v,u,x)=q_0(s)p(v,u|s)q_r(x|u,s):p(s,x)=q_0(s,x)\}
  \]
- **定理 1 内界**：满足
  \[
  R<I(V;X)-I(V;S),\qquad R_k>R+I(V;S,U|X)
  \]
  的速率对在凸包内可达。
- **定理 1 外界**：任意可达速率对必须满足
  \[
  R\le I(V;X)-I(V;S),\qquad R_k\ge R+I(V;S|X)
  \]
  对某个 \(p\in\mathcal P(q_0;q_r)\)。
- 辅助变量可限制为 \(|V|\le |S||U|+1\)。

### 3. 资源分解：覆盖、打包、潜协调

- 对固定可行分布，编码构造包含：
  - 覆盖：\(\hat R>I(V;S)\)，用于覆盖上下文序列 \(S^n\)。
  - 打包：\(R+\hat R<I(V;X)\)，用于消息与覆盖索引的联合解码。
  - 潜合成：\(R_k+\hat R>I(V;S,U)\)，用于在渲染器前协调物理潜变量。
- 令 \(\hat R=I(V;S)+t\)，则：
  \[
  R+t<I(V;X)-I(V;S),\qquad R_k+t>I(V;U|S)
  \]
  - 净打包预算
